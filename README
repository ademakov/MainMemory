Main Memory
===========

Main Memory is a substrate for networked servers of all sorts. However handling
of in-memory workloads is the top priority. For instance, it should be suitable
for creating a memcached clone with superior performance and scalability.
